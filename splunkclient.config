[{splunkclient, [
   {connections, [
     {splunkclient_conn_default,
       {protocol, "https"},
       {host, "localhost"},
       {port, "8089"},
       {user, "admin"},
       {pass, "changeme"}}]},
   {pools, [
     {splunkclient_pool_service,
       [ % size args
         {size, 10}, % max pool size
         {max_overflow, 5}], % max # of workers created if pool is empty
       [ % worker args
         ]}]
  }]
}].
